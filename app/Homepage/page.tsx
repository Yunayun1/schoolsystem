// app/Homepage/page.tsx

import Home from './Home/page';  // Importing the Home component

const Homepage = () => {
  return (
    <div>
      <Home />  {/* Render Home component on this page */}
    </div>
  );
};

export default Homepage;  // Make sure it's the default export
